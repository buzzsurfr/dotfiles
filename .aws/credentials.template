# AWS credentials are managed via the 1Password CLI plugin.
# The `aws` command is aliased to `op plugin run -- aws`, which injects
# credentials at runtime from 1Password without writing them to disk.
#
# To set up on a new machine:
#   op plugin init aws
#
# Then add to shell (already in .zshrc):
#   source ~/.config/op/plugins.sh
